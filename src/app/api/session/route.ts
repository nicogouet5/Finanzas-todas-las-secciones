import { NextResponse } from "next/server";
import { createSessionToken, sessionCookie, verifyCredentials } from "@/lib/admin-session";

export async function POST(request: Request) { let body: unknown; try { body = await request.json(); } catch { return NextResponse.json({ error: "Solicitud inválida" }, { status: 400 }); } if (!body || typeof body !== "object" || typeof (body as { user?: unknown }).user !== "string" || typeof (body as { password?: unknown }).password !== "string") return NextResponse.json({ error: "Solicitud inválida" }, { status: 400 }); try { if (!verifyCredentials((body as { user: string }).user, (body as { password: string }).password)) return NextResponse.json({ error: "Credenciales inválidas" }, { status: 401 }); const response = NextResponse.json({ ok: true }); response.cookies.set("hub_admin", createSessionToken(), sessionCookie); return response; } catch { return NextResponse.json({ error: "Configuración administrativa incompleta" }, { status: 503 }); } }
