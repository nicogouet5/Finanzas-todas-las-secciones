"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm() { const router = useRouter(); const [error, setError] = useState(""); const [pending, setPending] = useState(false); async function submit(formData: FormData) { setPending(true); setError(""); const response = await fetch("/api/session", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ user: formData.get("user"), password: formData.get("password") }) }); setPending(false); if (!response.ok) return setError("No fue posible iniciar sesión."); router.refresh(); } return <form className="login-form" action={submit}><label>Usuario<input name="user" autoComplete="username" required /></label><label>Contraseña<input name="password" type="password" autoComplete="current-password" required /></label>{error && <p role="alert">{error}</p>}<button className="button" disabled={pending}>{pending ? "Ingresando…" : "Ingresar"}</button></form>; }
